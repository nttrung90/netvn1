"use client";
import { Plus } from "lucide-react";
import type { Tag } from "@/types/database";
import { createTag, deleteTag } from "@/app/actions";
import { DeleteButton } from "./DeleteButton";
export function TagManager({ tags }: { tags: Tag[] }) { return <div className="grid gap-6 lg:grid-cols-[360px_minmax(0,1fr)]"><section className="h-fit rounded-2xl border bg-white p-5"><h2 className="font-bold">Thêm thẻ</h2><p className="mt-1 text-sm leading-6 text-[#667085]">Dùng thẻ để liên kết những chủ đề có tính giao thoa.</p><form action={createTag} className="mt-5 flex gap-2"><input name="name" required placeholder="Ví dụ: Mobile" className="min-w-0 flex-1 rounded-xl border border-[#d0d5dd] px-3 py-3 text-sm outline-none focus:border-[#4062ff]"/><button className="grid h-11 w-11 place-items-center rounded-xl bg-[#101828] text-white" aria-label="Thêm thẻ"><Plus size={17}/></button></form></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-bold">Tất cả thẻ</h2><p className="mt-1 text-xs text-[#667085]">{tags.length} thẻ đang được sử dụng.</p><div className="mt-5 flex flex-wrap gap-2">{tags.map((tag) => <div key={tag.id} className="inline-flex items-center rounded-full bg-[#edf0ff] pl-3 text-sm font-bold text-[#3346aa]"><span>#{tag.name}</span><DeleteButton label="" action={deleteTag.bind(null, tag.id)}/></div>)}{!tags.length && <p className="text-sm text-[#667085]">Chưa có thẻ.</p>}</div></section></div>; }
